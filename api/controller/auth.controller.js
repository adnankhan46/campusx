export const handleSignUp = (req, res)=>{
    res.json({message:"Working SignUP from auth controller"});
}

export const handleSignIn = (req, res)=>{
    res.json({message:"Working SignIN from auth controller"});
}